name: "example"
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2 # Checkout this project (including ./scripts dir)
      - uses: johnlindquist/kit-action@v1.0.3
        with:
          script: "./scripts/example-script.js" # The name of a script in your ./scripts dir
